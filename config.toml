# The URL the site will be built for
base_url = "https://blog.pwnspirit.xyz"

# The site title and description; used in feeds by default.
title = "Pwnspirit's Blog"
description = "Linux • Programming • Cybersecurity" 

# The default language; used in feeds.
default_language = "en"

# The site theme to use.
theme = "after-dark"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = false

# When set to "true", the generated HTML files are minified.
minify_html = false

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true

# When set to "true", a feed is automatically generated.
generate_feeds = true

# The filename to use for the feed. Used as the template filename, too.
feed_filenames = ["rss.xml"]

# The taxonomies to be rendered for the site and their configuration of the default languages
taxonomies = [{ name = "categories", feed = true }]

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true

# The theme to use for code highlighting.
highlight_theme = "axar"

# When set to "true", emoji aliases translated to their corresponding
# Unicode emoji equivalent in the rendered Markdown files. (e.g.: :smile: => 😄)
render_emoji = false

[search]
index_format = "elasticlunr_json"
include_title = true
include_description = true
include_content = true

[extra]
author = "Roshan Tiwaree"
after_dark_menu = [
  { name = "Home", url = "$BASE_URL" },
  { name = "Categories", url = "$BASE_URL/categories" },
  { name = "GitHub", url = "https://github.com/pwnspirit" },
  { name = "RSS", url = "$BASE_URL/rss.xml" },
  { name = "About", url = "https://pwnspirit.xyz" },
]
after_dark_title = "Pwnspirit's Blog | FOSS • Linux • Programming • Cybersecurity"
